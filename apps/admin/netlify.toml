[build]
  base = "/"
  publish = "apps/admin/dist/client"
  command = "turbo @remote-cache/admin#build migrate"

[dev]
  base = "/"
  port = 8888
  targetPort = 3000
  autoLaunch = false
  command = "turbo @remote-cache/admin#dev"
