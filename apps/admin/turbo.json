{
	"$schema": "../../node_modules/turbo/schema.json",
	"extends": ["//"],
	"tasks": {
		"migrate": {
			"env": [
				"ADMIN_NAME",
				"ADMIN_EMAIL",
				"ADMIN_PASSWORD",
				"BETTER_AUTH_SECRET",
				"DATABASE_URL"
			]
		},
		"build": {
			"env": ["NETLIFY", "SENTRY_AUTH_TOKEN", "SENTRY_ORG", "SENTRY_PROJECT"],
			"outputs": [
				".netlify/**",
				".nitro/**",
				".output/**",
				".tanstack/**",
				"dist/**"
			]
		},
		"lint": {
			"outputs": [".cache/eslint"]
		},
		"format": {
			"outputs": [".cache/prettier"]
		}
	}
}
